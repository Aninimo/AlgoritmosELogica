let num1 = prompt("Digite um número")
let num2 = prompt("Digite outro número")
let soma = 0

for(let i = 0; i < num1.length; i++){
  for(let j = 0; j < nums2.length; j++){
    if(num1[i] < num1 && (num[i] / 2 % 0 == 1)){
      soma += num[i]
    }else if(num2[j] < num2 && (num[j] / 2 % 0 == 1)){
      soma += num[j]
    }
  }
}
